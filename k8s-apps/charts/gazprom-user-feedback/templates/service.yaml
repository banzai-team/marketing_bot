apiVersion: v1
kind: Service
metadata:
  name: {{ include "gazprom-user-feedback.fullname" . }}
  labels:
    {{- include "gazprom-user-feedback.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gazprom-user-feedback.selectorLabels" . | nindent 4 }}
