apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gazprom-user-feedback.fullname" . }}-test-connection"
  labels:
    {{- include "gazprom-user-feedback.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gazprom-user-feedback.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
