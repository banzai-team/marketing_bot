server:
  port: ${SERVER_PORT}
spring:
  rabbitmq:
    host: ${RMQ_HOST}
    username: ${RMQ_USERNAME}
    password: ${RMQ_PASSWORD}
    template:
      routing-key: ${RMQ_ROUTING_KEY}
model:
  url: ${MODEL_URL}